import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.observerkei.chatbot',
  appName: 'ai-chatbot-starter',
  webDir: 'out'
};

export default config;
